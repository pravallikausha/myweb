in feture branch developers devlope the code nd merge the code into development team
to retify errors in code they create hotflux or bugflux branch nd clear the erorrs and merge to devlopmen team branch
from devlopment team branch they merge to master branch nd from master branch they push the code into remote repository after pushing the code into github 
they remove hotfulx branch
